{% block core %}{% from itertools import izip %}{{ "%20s" % "Username"}} {{ "%30s" % "User"}}{% if show_teams %}{{ "%30s" % "Team"}}{% end %} {% for task in contest.tasks %}{{ "%14s" % task.name }} {% end %}{{ "%8s" % "Global" }}
{% for p in sorted(contest.participations, key=lambda p: p.total_score, reverse=True) %}{% if not p.hidden %}{{ "%20s" % p.user.username }} {{ "%30s" % ("%s %s" % (p.user.first_name, p.user.last_name)) }}{% if show_teams %}{{ "%30s" % (p.team.name if p.team else "") }}{% end %} {% for (t_score, t_partial), task in izip(p.scores, contest.tasks) %}{{ ("%%13.%dlf" % task.score_precision) % t_score }}{% if t_partial %}*{% else %} {% end %} {% end %}{% set total_score, partial = p.total_score %}{{ ("%%7.%dlf" % contest.score_precision) % total_score }}{% if partial %}*{% else %} {% end %}
{% end %}{% end %}{% end %}
